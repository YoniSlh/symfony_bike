{% extends 'base.html.twig' %}

{% block title %}Détails du vélo{% endblock %}

{% block content %}
    {% include '_header.html.twig' %}
    <div class="veloDetailsContainer">
        <h1>🚴‍♂️ Détails du vélo : {{ velo.name }}</h1>
        <div class="veloDetails">
            <ul>
                <li><strong>Nom :</strong> {{ velo.name }}</li>
                <li><strong>Prix :</strong> {{ velo.price }} €</li>
                <li><strong>Type :</strong> {{ velo.type }}</li>
                <li><strong>Description :</strong> {{ velo.description }}</li>
                <li><img src="{{ velo.image }}" class="veloImageDetail"></li>
            </ul>
        </div>
        <div class="btn-container">
            <a href="{{ path('velos') }}" class="btnRedirection">Retour à la liste des vélos</a>
            <a href="{{ path('velos') }}" class="btnAjoutPanier">Ajouter au panier</a>
        </div>
    </div>
{% endblock %}
