{% block content %}
	<header class="siteHeader">
		<div class="container-nav">
			<h2 class="logoSite">
				<a href="{{ path('home') }}">Symfony Bike</a>
			</h2>
			<nav class="main-nav">
				<ul>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle">
							<img src="" class="lang-logo">
						</a>
						<ul class="dropdown-menu" style="display: none;">
							<li>
								<a href="{{ path('translate', {'lang': 'fr'}) }}">
									Français
								</a>
							</li>
							<li>
								<a href="{{ path('translate', {'lang': 'en'}) }}">
									English
								</a>
							</li>
						</ul>
					</li>
					<li>
						<a href="{{ path('app_admin') }}">Dashboard</a>
					</li>
					<li>
						<a href="{{ path('admin_users') }}">Utilisateurs</a>
					</li>
					<li>
						<a href="{{ path('admin_products') }}">Produits</a>
					</li>
					<li>
						<a href="{{ path('_logout_main') }}" onclick="return confirm('Êtes-vous sûr de vouloir vous déconnecter ?');">
							Se déconnecter
						</a>
					</li>
				</ul>
			</nav>
		</div>
	</header>
{% endblock %}
